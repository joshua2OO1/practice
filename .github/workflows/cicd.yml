name: Fail Workflow

on:
  pull_request:
    branches: [main]

jobs:
  fail-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      # - name: Fail step
      #   run: exit 1
